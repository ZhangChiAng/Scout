# 2026-09-02 按日过滤与多选正文验收

实施与验收日期：2026-09-06（北京时间）。范围仅为 2026-09-02 剩余 8 条。
使用真实橘鸦 RSS、现有模型端点、真实 SQLite 与原飞书群；未编写单元测试。

## 部署与常规发送

- 停用发送 timer，确认原 sender service 空闲；listener 保持运行。
- 增量初始化前保存本地 SQLite 备份；原 47 条正文送达、21 条反馈及档案 v7 保留。
- SQLite `integrity_check` 返回 `ok`，`foreign_key_check` 无异常。
- listener 重启，日志确认单个正文发送线程启动。
- 执行 `--send --issue-date 2026-09-02`：RSS 含 16 条，跳过前 8 条已送达正文，
  仅评价剩余 8 条。位置 10 为“不确定”，12:49:53 发送完整正文；其余 7 条为
  “不推荐”，随后发送列表 1，标题包含日期及 7 条计数。
- 发送命令返回 `sent=2 failed=0 skipped=8`（1 张正文、1 张列表）。
- 列表位置顺序为 9、11、12、13、14、15、16，成员固定于档案 v7 的判断。
- 同一期再次执行返回 `sent=0 failed=0 skipped=16`，没有重发列表或旧正文。
- 档案更新完成后执行同一期 `--dry-run`，返回零新增且前后 SQLite 文件 SHA-256
  一致，验证零写入。较早的 dry-run 与真实反馈同时发生，其文件变化来自 listener。
- 真正的卡片更新接口接受对列表 1 的刷新请求。

## 真实群交互

- 12:51:34 收到真实 `reveal_selected`，一次选择成员 2、5，即概览位置 11、14。
- 12:51:35、12:51:37 按原顺序送达两条正文，均仅尝试一次。
- SQLite 验证正文的快照、判断、理由与原列表完全一致；正文按原群 ID 发送。
- 列表更新到修订 3，数据库 `synced_revision=3`，已送达成员禁用；其余 5 条可选。
- 12:51:59 重启 listener，12:52:03 后继续运行，已完成请求没有重发。
- 真实反馈修订 22 来自位置 10；修订 23、24 来自展开的两条正文，其中 23、24
  在 listener 重启后正常入库。查看正文自身未产生反馈修订。
- 再次运行同一期后，档案 v8 已激活并通知；其证据包含 22、23、24，已处理修订
  为 24。新闻仍返回 `sent=0 failed=0 skipped=16`。原列表成员仍保存 v7 的评价。

## 验证边界与后续

飞书应用未启用读取消息权限，消息回读返回 `99991672`。本次通过发送及更新
接口成功响应、真实 owner 回调、送达与反馈记录核验；不以消息读取失败代替
交互失败，也未额外申请应用权限。

重复提交、追加选择、发送失败三次后的重新选择、发送中断后的请求恢复、长列表分页、没有不推荐及
全是不推荐等分支已实现，但本期真实数据未触发，留待后续真实日报验证。
不伪造消息、回调或偏好，不为覆盖这些分支发送其他日期。

静态检查：`ruff check .`、`ruff format --check .`、`compileall -q scout`、
`git diff --check` 均通过。

观察期间发送 timer 保持停用，listener 常驻。下一期及 timer 恢复等待 owner 反馈。
