# 知乎话题与反馈学习验收

本轮只使用真实知乎登录会话、真实 SQLite 和配置的真实飞书群；未编写单元或模拟测试。
人工标签由 owner 在飞书提交，本报告不把静态检查或采集器局部成功写成完整闭环成功。

用户决定本轮验收到此为止：字面关键词过滤能力不足，当前功能保留为试用，未达到生产上线
要求。每日自动扫描保持关闭。以下证据仅证明已覆盖的采集、投递与学习流程，不代表内容
质量或过滤效果达标。

## 已完成

- 采集器升级已获用户明确授权，先备份 SQLite 再安装并重启；真实健康接口声明
  `question_answers`，登录会话重新核验 verified。
- 真实问题默认列表已核对 5 条（列表自然结束）和 20 条（4 页），原始顺序、不同回答 ID、
  列表排名、question_id 和证据 SHA256 全部一致。同 UUID 的运行中/完成后重复请求返回原任务，
  参数变化复用 UUID 返回 409。
- 真实采集进程在首个列表页持久化后中断，以原 UUID、原分页游标恢复到 20 条，首屏证据未重取。
  具体结果见 [采集器报告](collector-upgrade/QUESTION_ANSWERS_ACCEPTANCE.md)。
- Scout 在 sender 锁内创建真实迁移备份
  `data/scout-before-zhihu-topics-1790424781270044306.sqlite3`。备份和迁移后数据库
  integrity_check=ok，foreign_key_check 无违规。443 个旧正文快照、46 条橘鸦反馈、20 个偏好
  版本、3 条历史知乎送达及原知乎任务逐项保留，详见 [迁移证据](zhihu-migration-evidence.json)。
- Scout listener 已重启运行，管理卡首次发送成功且 SQLite 记账：卡片队列 ID `1`，发送 UUID
  `2ab10f66-111c-46d8-b98a-87b51042c919`，飞书消息
  `om_x100b644f37d0b8a4b26f388276de05c`。
- owner 通过真实管理卡新增话题 `GPT-6`（ID 1），搜索词 `gpt6`、`gpt-6`，近 30 天，随后提交
  立即扫描。后台创建扫描 `a9c1d18e-f652-5119-a6e5-b9b559c2a7da`。每日计划未启用，时刻未预设。
- Scout 运行中重启后保留原扫描和原采集任务，已经完成的列表保持不变，见
  [扫描恢复证据](zhihu-scan-restart-evidence.json)。
- 默认列表完整正文复用已用真实 API 核对：5篇全文与独立详情逐字及SHA一致；一次20条的顺序
  与此前4页结果一致。生产接口和Scout完整性校验均通过，见
  [正文复用证据](collector-upgrade/production-body-results.json)。
- 用户补充搜索词忽略大小写，已在保存和新扫描查询构造两处casefold去重，并上线。
- 用户在本轮扫描期间要求发送 10 篇，已明确修订当前扫描的max_results快照及后续新扫描默认值；校准阶段也采用10篇。
- 真实话题扫描已完成：99 个搜索种子触发 46 个问题扩展，取得 624 条同题列表记录，合并后
  675 个候选。全部 675 篇取得完整正文，618 篇通过筛选，57 篇因发布时间过期被筛除，
  历史送达去重数为 0，采集错误为 0。
- 46 个问题的原始分页顺序、回答身份、连续排名与证据哈希均通过核对；其中 24 个问题返回
  20 篇，22 个不足 20 篇。9 个前 20 名之外的种子仍被保留，11 个被多个种子命中的问题
  均只扩展一次，2 个过期种子仍触发扩展；过期筛除后没有以第 21 篇补位。
- 按当前扫描冻结的校准状态（学习版本为空，候选得分为 0）和发布时间排序取前 10 篇，
  10 篇均已成功送达且有飞书消息 ID，身份键与发送 UUID 无重复。候选边界及投递结果见
  [真实候选证据](zhihu-candidate-evidence.json)。
- owner 已在飞书提交 1 条喜欢、2 条不喜欢反馈，后台生成学习版本 3；两个降权词的权重均为
  -0.9566257518157718，阈值为 0。当前 3 个训练样本可分，但样本内拟合不代表生产过滤能力。
  原扫描仍保留空学习版本，不受新反馈改写。
- Scout Ruff、格式、编译及 git diff --check 通过；采集器 Ruff 与 AST 检查通过。

## 已暂停的验收

按用户要求停止后续人工操作与验收。反馈修改、过滤结果补标等尚未完成的场景如实保留在
下方清单，不再等待用户继续打标。

## 尚未覆盖

- 历史已发送种子仍扩展；列表内已发送或低分内容筛除后不补第 21 个。
- 重复扫描、跨话题及重启后的自动送达去重，URL或标题变化后的稳定身份。
- 人工提交未命中正文的降权词时被拒绝；反馈修改替换当前标签，过滤结果补标重新训练。
- 实际训练失败保留有效版本、相反标签特征冲突、反馈修改后退回单类样本。
- 飞书发送途中进程中断与原 UUID 恢复、超出远端幂等窗口的未知发送结果。
- 每日配置时刻到达后的实际自动触发、真实会话失效、部分扩展失败、默认排名跨日变化。

生产中不存在以上场景时，不构造模拟响应或伪造人工标签代替验收。
